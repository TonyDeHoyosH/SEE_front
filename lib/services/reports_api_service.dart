import '../config/api_config.dart';
import 'base_api_service.dart';

class HttpReportsApiService implements ReportsApiService {
  // ApiClient se instanciará aquí al implementar los endpoints de reportes.
  // ignore: unused_field
  static final String _baseUrl = ApiConfig.reportsUrl;

  // Endpoints de reportes — se implementarán en iteraciones futuras
}
